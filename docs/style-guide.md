How are IDs formatted?

The ID is used to categorize each word entry. Every word will be linked to other words.

Do we use snake_case or camelCase?

Snake case works well.

How do we name database columns?

How do we store Arabic roots?

I'm thinking this can be another tag, but that doesn't work. I'll use tag functionality here, but the tag will lead to a central page where the root links to all the other words I added.

How do we represent dialects?

Likely with tags.

When do we use vowelized Arabic?

Vocalized Arabic will come up in the search results. Practically speaking, I need it to differentiate different words. An Arabic word can share the same roots, but different placements. I'll use vowels everywhere to tell what a word means. Additionally, I'll have to link the vowelized words to the same unvowelled placeholder word.
